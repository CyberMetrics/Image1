Flask
numpy
joblib
# Use specific CPU version to avoid downloading massive GPU binaries which might spike memory/disk
torch --index-url https://download.pytorch.org/whl/cpu
scikit-learn
# sentence-transformers is heavy. We might need a lighter alternative or stick with it but hope CPU torch saves us.
sentence-transformers
